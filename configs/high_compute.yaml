project:
  run_name: "credit_shap_lime_high_compute"
  random_seed: 42

models:
  use_smote_in_cv: true
  compare_imbalance_strategies: true
  tune_hyperparameters: true
  tuning_iterations: 40

explain:
  use_full_test_for_explanations: true
  sample_size: 3000
  lime_num_samples: 5000
  lime_num_samples_sweep: [500, 1000, 5000]
  lime_seed_count: 50

stability:
  bootstrap_samples: 1000

scalability:
  sizes: [100, 300, 500, 1000, 2500, 5000, 10000, 22500]
  repeats: 5
